name: flutter_classroom
description: Flutter Classroom

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.3
  equatable: ^2.0.3
  http: ^0.13.4
  flutter_bloc: ^7.3.0
  logger: ^1.1.0
  flutter_svg: ^0.23.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.1.4
  mockito: ^5.0.16
  flutter_lints: ^1.0.4
  bloc_test: ^8.3.0

flutter:
  uses-material-design: true

  assets:
    - assets/flags/